import { Example } from "@/screens/docs/parts/example";
import { PropsTable } from "@/screens/docs/parts/props-table";
import CheckboxControlledDemo from "./demos/checkbox/controlled";
import CheckboxControlledSource from "./demos/checkbox/controlled?raw";
import CheckboxDisabledDemo from "./demos/checkbox/disabled";
import CheckboxDisabledSource from "./demos/checkbox/disabled?raw";
import CheckboxGroupDemo from "./demos/checkbox/group";
import CheckboxGroupSource from "./demos/checkbox/group?raw";
import CheckboxIndeterminateDemo from "./demos/checkbox/indeterminate";
import CheckboxIndeterminateSource from "./demos/checkbox/indeterminate?raw";
import CheckboxUsageDemo from "./demos/checkbox/usage";
import CheckboxUsageSource from "./demos/checkbox/usage?raw";

export const PARTS = [
  {
    name: "Checkbox.Root",
    description:
      "外枠。 label と control を gap-2 で並べる。 HiddenInput を内部で自動 mount するので form 連携が効く。",
  },
  {
    name: "Checkbox.Control",
    description:
      "実際の四角。 size-4 + border、 checked / indeterminate で bg-fg に切替。 Indicator を内蔵 (default は Check / Minus)。",
  },
  {
    name: "Checkbox.Label",
    description: "右の text。 クリックで toggle するので clickable area が広がる。",
  },
  {
    name: "Checkbox.Group",
    description:
      "複数 checkbox を value array で管理。 flex flex-col gap-2 で縦並び。",
  },
];

# Checkbox

on / off / indeterminate の 3 状態を持つ form control。 Ark UI ベース。 単独 checkbox は Root + Control + Label の 3 部、 複数同時管理は Group で囲んで value array に流す。

## Usage

uncontrolled (defaultChecked) の最小例。 Label クリックでも toggle。

<Example code={CheckboxUsageSource}>
  <CheckboxUsageDemo />
</Example>

## Import

```tsx
import { Checkbox } from "@/components/ui/checkbox";
```

```tsx
<Checkbox.Root>
  <Checkbox.Control />
  <Checkbox.Label>...</Checkbox.Label>
</Checkbox.Root>
```

## Controlled

checked + onCheckedChange を渡して値を制御。

<Example code={CheckboxControlledSource}>
  <CheckboxControlledDemo />
</Example>

## Indeterminate

'indeterminate' で 中間状態。 子 checkbox の一部だけ選択中の親 checkbox に使う。

<Example code={CheckboxIndeterminateSource}>
  <CheckboxIndeterminateDemo />
</Example>

## Disabled

disabled で操作不可 + opacity-50。

<Example code={CheckboxDisabledSource}>
  <CheckboxDisabledDemo />
</Example>

## Group

Checkbox.Group で value array を一括管理。 各 Root に value を渡す。

<Example code={CheckboxGroupSource}>
  <CheckboxGroupDemo />
</Example>

## Anatomy

Root の中に Control と Label を並べる。 HiddenInput は Root が自動 mount。

<ul className="flex flex-col gap-2 rounded-md border border-border p-4">
  {PARTS.map((p) => (
    <li key={p.name} className="grid grid-cols-[10rem_1fr] gap-3">
      <span className="font-mono text-fg text-xs">{p.name}</span>
      <span className="text-fg-muted text-xs leading-relaxed">
        {p.description}
      </span>
    </li>
  ))}
</ul>

## Root props

<PropsTable
  rows={[
    {
      name: "checked",
      type: "boolean | 'indeterminate'",
      description: "controlled mode の値。 'indeterminate' で中間状態。",
    },
    {
      name: "defaultChecked",
      type: "boolean | 'indeterminate'",
      description: "uncontrolled mode の初期値。",
    },
    {
      name: "onCheckedChange",
      type: "(details: { checked: boolean | 'indeterminate' }) => void",
      description: "値が変わった時に呼ばれる。 controlled mode で必須。",
    },
    {
      name: "disabled",
      type: "boolean",
      default: "false",
      description: "操作不可。 opacity-50 + cursor-not-allowed。",
    },
    {
      name: "invalid",
      type: "boolean",
      default: "false",
      description: "form-level error 表示。 aria-invalid に流れる。",
    },
  ]}
/>
