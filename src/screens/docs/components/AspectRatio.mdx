import { Example } from "@/screens/docs/parts/example";
import { PropsTable } from "@/screens/docs/parts/props-table";
import AspectRatioCommonRatiosDemo from "./demos/aspect-ratio/common-ratios";
import AspectRatioCommonRatiosSource from "./demos/aspect-ratio/common-ratios?raw";
import AspectRatioUsageDemo from "./demos/aspect-ratio/usage";
import AspectRatioUsageSource from "./demos/aspect-ratio/usage?raw";
import AspectRatioVideoFrameDemo from "./demos/aspect-ratio/video-frame";
import AspectRatioVideoFrameSource from "./demos/aspect-ratio/video-frame?raw";

# AspectRatio

子要素を固定比率の box に閉じ込める単純 wrapper。 CSS の aspect-ratio property を ratio prop で受けるだけ。 中身は relative box の中に絶対配置 / 普通の flow どちらも置ける。

## Usage

ratio に幅÷高さを渡す。 16:9 なら 16/9、 正方形なら 1。

<Example code={AspectRatioUsageSource}>
  <AspectRatioUsageDemo />
</Example>

## Import

```tsx
import { AspectRatio } from "@/components/ui/aspect-ratio";
```

```tsx
<AspectRatio ratio={16 / 9}>...</AspectRatio>
```

## Common ratios

grid で並べると同じ幅の中で高さだけ変わるのが見やすい。

<Example code={AspectRatioCommonRatiosSource}>
  <AspectRatioCommonRatiosDemo />
</Example>

## Image / video frame

thumbnail を中に置き、 absolute で overlay を重ねる典型例。 box は relative なので absolute 配置がそのまま使える。

<Example code={AspectRatioVideoFrameSource}>
  <AspectRatioVideoFrameDemo />
</Example>

## Props

<PropsTable
  rows={[
    {
      name: "ratio",
      type: "number",
      default: "1",
      description:
        "横/縦の比。 16/9 や 4/3 のように分数で渡す。 CSS aspect-ratio に流す。",
    },
    {
      name: "className",
      type: "string",
      default: "—",
      description:
        "default で relative + w-full + overflow-hidden が当たる。 重ねて rounded / border 等を追加。",
    },
  ]}
/>
