import { Example } from "@/screens/docs/parts/example";
import { PropsTable } from "@/screens/docs/parts/props-table";
import SpinnerColorsDemo from "./demos/spinner/colors";
import SpinnerColorsSource from "./demos/spinner/colors?raw";
import SpinnerFullPanelDemo from "./demos/spinner/full-panel";
import SpinnerFullPanelSource from "./demos/spinner/full-panel?raw";
import SpinnerInButtonDemo from "./demos/spinner/in-button";
import SpinnerInButtonSource from "./demos/spinner/in-button?raw";
import SpinnerInIconButtonDemo from "./demos/spinner/in-icon-button";
import SpinnerInIconButtonSource from "./demos/spinner/in-icon-button?raw";
import SpinnerSizesDemo from "./demos/spinner/sizes";
import SpinnerSizesSource from "./demos/spinner/sizes?raw";
import SpinnerUsageDemo from "./demos/spinner/usage";
import SpinnerUsageSource from "./demos/spinner/usage?raw";

# Spinner

loading 中の汎用 indicator。 Lucide Loader2 を回転 animation で wrap した SVG。 size と color は className で。 Button / IconButton の中に置けば submit pending、 単体置きで全画面 loading にも。

## Usage

単体は class 無しで size-4 の標準サイズ。

<Example code={SpinnerUsageSource}>
  <SpinnerUsageDemo />
</Example>

## Import

```tsx
import { Spinner } from "@/components/ui/spinner";
```

```tsx
<Spinner />
```

## Sizes

size-\* で大きさを切り替え。 周辺の text や button に合わせる。

<Example code={SpinnerSizesSource}>
  <SpinnerSizesDemo />
</Example>

## Colors

text-\* で色付け。 fg / fg-soft / fg-muted を context に合わせて。

<Example code={SpinnerColorsSource}>
  <SpinnerColorsDemo />
</Example>

## In Button

submit pending の典型。 label と並べる時は size-4、 label を残すか差し替えるかは UI 次第。

<Example code={SpinnerInButtonSource}>
  <SpinnerInButtonDemo />
</Example>

## In IconButton

refresh / sync button の処理中。 同 size の Spinner に差し替える。

<Example code={SpinnerInIconButtonSource}>
  <SpinnerInIconButtonDemo />
</Example>

## Full panel

card / section 全体の loading。 中央に大きめ + 補足 text。

<Example code={SpinnerFullPanelSource}>
  <SpinnerFullPanelDemo />
</Example>

## Props

<PropsTable
  rows={[
    {
      name: "className",
      type: "string",
      description:
        "size-* で大きさ、 text-* で色。 default は size-4 text-fg-muted。",
    },
    {
      name: "...SVGProps",
      type: "—",
      description:
        "Lucide Loader2 (svg) の prop を pass-through。 role=status / aria-label=Loading は default で付く。",
    },
  ]}
/>
