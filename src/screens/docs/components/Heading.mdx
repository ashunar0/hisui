import { Example } from "@/screens/docs/parts/example";
import { PropsTable } from "@/screens/docs/parts/props-table";
import HeadingAsTagDemo from "./demos/heading/as-tag";
import HeadingAsTagSource from "./demos/heading/as-tag?raw";
import HeadingPageTitleDemo from "./demos/heading/page-title";
import HeadingPageTitleSource from "./demos/heading/page-title?raw";
import HeadingSectionLabelDemo from "./demos/heading/section-label";
import HeadingSectionLabelSource from "./demos/heading/section-label?raw";
import HeadingSizesDemo from "./demos/heading/sizes";
import HeadingSizesSource from "./demos/heading/sizes?raw";
import HeadingUsageDemo from "./demos/heading/usage";
import HeadingUsageSource from "./demos/heading/usage?raw";

# Heading

見出し。 font-weight は semibold 固定、 size と as を独立に持つのが特徴。 page title は `as="h1"` + `size="2xl"` のように見た目と semantics を分けて指定する。

## Usage

default は as=h2 / size=md。 何も指定しなければ section 見出し相当。

<Example code={HeadingUsageSource}>
  <HeadingUsageDemo />
</Example>

## Import

```tsx
import { Heading } from "@/components/ui/heading";
```

```tsx
<Heading as="h1" size="2xl">Title</Heading>
```

## Sizes

8 段階。 3xl 以上は自動で tracking-tight が付く。

<Example code={HeadingSizesSource}>
  <HeadingSizesDemo />
</Example>

## As (semantic tag)

size と独立に HTML tag を選ぶ。 a11y / SEO の文書構造はここで決める。

<Example code={HeadingAsTagSource}>
  <HeadingAsTagDemo />
</Example>

## Page title pattern

page の H1 は size=2xl + as=h1。 hero では 3xl 以上を使う。

<Example code={HeadingPageTitleSource}>
  <HeadingPageTitleDemo />
</Example>

## Section label pattern

docs の section 見出しは as=h2 + size=sm + text-fg-soft で控えめに。

<Example code={HeadingSectionLabelSource}>
  <HeadingSectionLabelDemo />
</Example>

## Props

<PropsTable
  rows={[
    {
      name: "size",
      type: '"xs" | "sm" | "md" | "lg" | "xl" | "2xl" | "3xl" | "4xl"',
      default: '"md"',
      description:
        "見た目のサイズ。 3xl 以上は tracking-tight が自動で付く。 詳細は Foundations/Typography。",
    },
    {
      name: "as",
      type: '"h1" | "h2" | "h3" | "h4" | "h5" | "h6"',
      default: '"h2"',
      description:
        "出力する HTML tag。 見た目 (size) と semantics (as) を独立に指定する。",
    },
  ]}
/>
