import { Example } from "@hisui/screens/docs/parts/example";
import { PropsTable } from "@hisui/screens/docs/parts/props-table";
import PopoverBasicDemo from "@hisui/screens/docs/components/demos/popover/basic";
import PopoverHelpIconDemo from "@hisui/screens/docs/components/demos/popover/help-icon";
import PopoverPlacementsDemo from "@hisui/screens/docs/components/demos/popover/placements";
import PopoverSwitchPanelDemo from "@hisui/screens/docs/components/demos/popover/switch-panel";
import PopoverWithCloseDemo from "@hisui/screens/docs/components/demos/popover/with-close";
import PopoverWithFormDemo from "@hisui/screens/docs/components/demos/popover/with-form";
import { loadDemo } from "@/lib/read-source";

export const popoverBasic = await loadDemo("popover/basic.tsx");
export const popoverHelpIcon = await loadDemo("popover/help-icon.tsx");
export const popoverPlacements = await loadDemo("popover/placements.tsx");
export const popoverSwitchPanel = await loadDemo("popover/switch-panel.tsx");
export const popoverWithClose = await loadDemo("popover/with-close.tsx");
export const popoverWithForm = await loadDemo("popover/with-form.tsx");
export const metadata = { title: "Popover | Hisui UI" };

export const PARTS = [
  {
    name: "Popover.Root",
    description:
      "外枠。 open / onOpenChange で開閉を制御。 modal default は false (背景操作可)。",
  },
  {
    name: "Popover.Trigger",
    description: "開閉ボタン。 asChild で Button / IconButton 等にラップ。",
  },
  {
    name: "Popover.Anchor",
    description:
      "位置の基準を Trigger と切り離したい時に使う。 通常は不要 (Trigger が anchor)。",
  },
  {
    name: "Popover.Content",
    description:
      "中身。 内部で Portal + Positioner を自動 mount。 multi-layer shadow の floating surface。",
  },
  {
    name: "Popover.Title",
    description: "見出し。 a11y のため aria-labelledby に自動接続。",
  },
  {
    name: "Popover.Description",
    description: "補足。 aria-describedby に自動接続。",
  },
  {
    name: "Popover.Arrow / ArrowTip",
    description:
      "矢印。 Content の中に Arrow を置くと placement に追従して向きが変わる。",
  },
  {
    name: "Popover.CloseTrigger",
    description: "閉じるボタン。 IconButton をラップして × で閉じる典型。",
  },
];

# Popover

Trigger に紐づく floating panel。 Tooltip より中身が自由 (form / button / rich content) で、 click で開閉する。 Dialog と違い modal=false が default なので背景操作も継続できる。

## Usage

Trigger を Button、 Content に Title + Description + 中身を並べる典型。

<Example code={popoverBasic.code} html={popoverBasic.html}>
  <PopoverBasicDemo />
</Example>

## Import

```tsx
import { Popover } from "@/components/ui/popover";
```

```tsx
<Popover.Root>
  <Popover.Trigger asChild>
    <Button />
  </Popover.Trigger>
  <Popover.Content>
    <Popover.Title />
    <Popover.Description />
  </Popover.Content>
</Popover.Root>
```

## With form

Input + Button を Content に並べて、 inline 編集 popup を組む。

<Example code={popoverWithForm.code} html={popoverWithForm.html}>
  <PopoverWithFormDemo />
</Example>

## With close button

Content 右上に CloseTrigger + IconButton を absolute で配置する典型 (notification panel など)。

<Example code={popoverWithClose.code} html={popoverWithClose.html}>
  <PopoverWithCloseDemo />
</Example>

## Placements

positioning.placement で 4 方向。 余白が無いと自動 flip。 Arrow / ArrowTip を Content に入れると向きに追従。

<Example code={popoverPlacements.code} html={popoverPlacements.html}>
  <PopoverPlacementsDemo />
</Example>

## Switch panel

Switch / Checkbox を並べて inline 設定。 click 外しで自動閉じる挙動が popover 向き。

<Example code={popoverSwitchPanel.code} html={popoverSwitchPanel.html}>
  <PopoverSwitchPanelDemo />
</Example>

## On help icon

Tooltip より中身が長い説明 / link / formatting が欲しい時の form helper。

<Example code={popoverHelpIcon.code} html={popoverHelpIcon.html}>
  <PopoverHelpIconDemo />
</Example>

## Anatomy

Root の中に Trigger と Content。 Content は中で Portal + Positioner を内蔵。

<ul className="flex flex-col gap-2 rounded-md border border-border p-4">
  {PARTS.map((p) => (
    <li key={p.name} className="grid grid-cols-[14rem_1fr] gap-3">
      <span className="font-mono text-fg text-xs">{p.name}</span>
      <span className="text-fg-muted text-xs leading-relaxed">
        {p.description}
      </span>
    </li>
  ))}
</ul>

## Root props

<PropsTable
  rows={[
    {
      name: "open",
      type: "boolean",
      description: "controlled mode の表示状態。",
    },
    {
      name: "defaultOpen",
      type: "boolean",
      default: "false",
      description: "uncontrolled mode の初期表示状態。",
    },
    {
      name: "onOpenChange",
      type: "(details: { open: boolean }) => void",
      description: "open 状態が変わった時に呼ばれる。",
    },
    {
      name: "modal",
      type: "boolean",
      default: "false",
      description:
        "Dialog と違い default は false (背景操作可)。 true で focus trap + body lock。",
    },
    {
      name: "positioning",
      type: '{ placement?: "top" | "bottom" | "left" | "right" | ... }',
      description:
        "Floating UI 設定。 placement / gutter / offset 等を渡す。",
    },
    {
      name: "closeOnInteractOutside",
      type: "boolean",
      default: "true",
      description: "外クリックで閉じるか。",
    },
    {
      name: "closeOnEscape",
      type: "boolean",
      default: "true",
      description: "Esc キーで閉じるか。",
    },
  ]}
/>
