import { Example } from "@hisui/screens/docs/parts/example";
import { PropsTable } from "@hisui/screens/docs/parts/props-table";
import ProgressCircularDemo from "@hisui/screens/docs/components/demos/progress/circular";
import ProgressCircularLabelDemo from "@hisui/screens/docs/components/demos/progress/circular-label";
import ProgressIndeterminateDemo from "@hisui/screens/docs/components/demos/progress/indeterminate";
import ProgressLinearDemo from "@hisui/screens/docs/components/demos/progress/linear";
import ProgressStateViewDemo from "@hisui/screens/docs/components/demos/progress/state-view";
import { loadDemo } from "@/lib/read-source";

export const progressCircular = await loadDemo("progress/circular.tsx");
export const progressCircularLabel = await loadDemo("progress/circular-label.tsx");
export const progressIndeterminate = await loadDemo("progress/indeterminate.tsx");
export const progressLinear = await loadDemo("progress/linear.tsx");
export const progressStateView = await loadDemo("progress/state-view.tsx");
export const metadata = { title: "Progress | Hisui UI" };

export const PARTS = [
  {
    name: "Progress.Root",
    description:
      "外枠。 value / min / max を持つ。 value={null} で indeterminate。 default は flex flex-col gap-1.5。",
  },
  {
    name: "Progress.Label",
    description: "見出し。 text-xs + font-medium。",
  },
  {
    name: "Progress.ValueText",
    description:
      "現在値の表示。 children を直接渡す (`<Progress.ValueText>{value}%</Progress.ValueText>`)。 styling だけ提供。",
  },
  {
    name: "Progress.Track",
    description: "linear の枠。 bg-surface-sunken の rounded bar。",
  },
  {
    name: "Progress.Range",
    description:
      "Track 内の塗り。 data-state=indeterminate で animation 切替。",
  },
  {
    name: "Progress.Circle",
    description:
      "circular SVG container。 size / thickness を numeric props で指定 (default 48 / 4)。",
  },
  {
    name: "Progress.CircleTrack",
    description: "circular の背景円。 stroke-surface-sunken。",
  },
  {
    name: "Progress.CircleRange",
    description: "circular の塗り円。 stroke-fg。",
  },
];

# Progress

進捗を表す bar / circle。 1 つの primitive で linear と circular 両方サポート。 value={null} で indeterminate animation に切替。 server-safe (RSC で動く)、 JS なしで描画。

## Usage

Track + Range の bar。 Label / ValueText を上に並べるのが定型。

<Example code={progressLinear.code} html={progressLinear.html}>
  <ProgressLinearDemo />
</Example>

## Import

```tsx
import { Progress } from "@/components/ui/progress";
```

```tsx
<Progress.Root value={65}>
  <Progress.Label>uploading…</Progress.Label>
  <Progress.ValueText>65%</Progress.ValueText>
  <Progress.Track>
    <Progress.Range />
  </Progress.Track>
</Progress.Root>
```

## Indeterminate

value={null} で進捗不明モード。 Range が data-state=indeterminate になり muted 色 + animation に切替。

<Example code={progressIndeterminate.code} html={progressIndeterminate.html}>
  <ProgressIndeterminateDemo />
</Example>

## Circular

Circle + CircleTrack + CircleRange の SVG。 size / thickness props で大きさを指定。

<Example code={progressCircular.code} html={progressCircular.html}>
  <ProgressCircularDemo />
</Example>

## Circular with center label

Circle を relative box で包んで absolute inset-0 で中央に label を重ねる。 Apple Watch 風の見せ方。

<Example code={progressCircularLabel.code} html={progressCircularLabel.html}>
  <ProgressCircularLabelDemo />
</Example>

## State swap

value が max に達したかは user 側で判定して content を切替。 100% で Done badge を見せたい時のパターン。

<Example code={progressStateView.code} html={progressStateView.html}>
  <ProgressStateViewDemo />
</Example>

## Anatomy

Root に value を渡し、 linear なら Track + Range、 circular なら Circle + CircleTrack + CircleRange を組む。

<ul className="flex flex-col gap-2 rounded-md border border-border p-4">
  {PARTS.map((p) => (
    <li key={p.name} className="grid grid-cols-[14rem_1fr] gap-3">
      <span className="font-mono text-fg text-xs">{p.name}</span>
      <span className="text-fg-muted text-xs leading-relaxed">
        {p.description}
      </span>
    </li>
  ))}
</ul>

## Root props

<PropsTable
  rows={[
    {
      name: "value",
      type: "number | null",
      default: "0",
      description: "現在値。 null で indeterminate (無限 animation)。",
    },
    {
      name: "min",
      type: "number",
      default: "0",
      description: "最小値。",
    },
    {
      name: "max",
      type: "number",
      default: "100",
      description: "最大値。",
    },
  ]}
/>
