import { Example } from "@hisui/screens/docs/parts/example";
import { PropsTable } from "@hisui/screens/docs/parts/props-table";
import SkeletonProfileCardDemo from "@hisui/screens/docs/components/demos/skeleton/profile-card";
import SkeletonShapesDemo from "@hisui/screens/docs/components/demos/skeleton/shapes";
import SkeletonStatGridDemo from "@hisui/screens/docs/components/demos/skeleton/stat-grid";
import SkeletonTableRowsDemo from "@hisui/screens/docs/components/demos/skeleton/table-rows";
import SkeletonUsageDemo from "@hisui/screens/docs/components/demos/skeleton/usage";
import { loadDemo } from "@/lib/read-source";

export const skeletonProfileCard = await loadDemo("skeleton/profile-card.tsx");
export const skeletonShapes = await loadDemo("skeleton/shapes.tsx");
export const skeletonStatGrid = await loadDemo("skeleton/stat-grid.tsx");
export const skeletonTableRows = await loadDemo("skeleton/table-rows.tsx");
export const skeletonUsage = await loadDemo("skeleton/usage.tsx");
export const metadata = { title: "Skeleton | Hisui UI" };

# Skeleton

読み込み中の placeholder。 size / shape は className で指定する。 中身は無く、 animate-pulse の脈打つ bg-surface-sunken のみ。 1 つの primitive を組み合わせて card / row / grid の loading 状態を組む。

## Usage

単体は width / height を class で指定するだけ。

<Example code={skeletonUsage.code} html={skeletonUsage.html}>
  <SkeletonUsageDemo />
</Example>

## Import

```tsx
import { Skeleton } from "@/components/ui/skeleton";
```

```tsx
<Skeleton className="h-4 w-40" />
```

## Shapes

rounded-full で円、 rounded-md で button / chip 風、 h-1 + w-full で text 行。

<Example code={skeletonShapes.code} html={skeletonShapes.html}>
  <SkeletonShapesDemo />
</Example>

## Profile card

avatar + name + email + bio 2 行 + action button。 bio の width 違いで実 text の揺らぎを再現。

<Example code={skeletonProfileCard.code} html={skeletonProfileCard.html}>
  <SkeletonProfileCardDemo />
</Example>

## Table rows

header + 5 行。 各列で width をずらして実データの不揃いを表現。

<Example code={skeletonTableRows.code} html={skeletonTableRows.html}>
  <SkeletonTableRowsDemo />
</Example>

## Stat grid

Dashboard widget の loading。 数字行は h-7 大きめ、 補足行は h-3 + 60% 幅で hierarchy を残す。

<Example code={skeletonStatGrid.code} html={skeletonStatGrid.html}>
  <SkeletonStatGridDemo />
</Example>

## Props

<PropsTable
  rows={[
    {
      name: "className",
      type: "string",
      description:
        "形状指定はここで。 width / height / rounded-* を Tailwind class で当てる。",
    },
    {
      name: "...HTMLAttributes<div>",
      type: "—",
      description:
        "div の全 prop を pass-through。 id / style / aria-* 等。 aria-hidden=true は default で付く。",
    },
  ]}
/>
